[Unit]
Description=MTProto Proxy
After=network.target

[Service]
Type=simple
ExecStart=/bin/sh -c '/usr/local/bin/mtg run --bind 0.0.0.0:443 $(cat /etc/mtg/secrets.txt | tr "\\n" " ")'
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
